import type { ValidationCode } from './types';

/**
 * IGSL validation engine — runs against canvas state
 * Checks governance blocks and edges for IGSL grammar compliance
 * 
 * TODO: Implement in Phase A Week 3
 */
export function validateProtocol(blocks: unknown[], edges: unknown[]): ValidationCode[] {
  return [];
}
